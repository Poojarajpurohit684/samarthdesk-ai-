{
  "$schema": "https://railway.com",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd frontend && npm run build && vite preview --port $PORT --host 0.0.0.0",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
